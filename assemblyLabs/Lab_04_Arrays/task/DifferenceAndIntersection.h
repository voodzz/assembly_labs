#pragma once
#ifndef DIFFERENCE_AND_INTERSECTION_H
#define DIFFERENCE_AND_INTERSECTION_H

#include <map>
#include <string>
#include <vector>

std::map<std::string, bool> isDifferenceIsIntersection(std::vector<int>& inspected, std::vector<int>& arr1, std::vector<int>& arr2);

#endif // !DIFFERENCE_AND_INTERSECTION_H
